"""A file that contains the Chart dataclass."""

from dataclasses import dataclass
from typing import Dict


@dataclass
class Chart:
    """A dataclass that defines a chart.
    
    Serializes and deserializes to JSON.
    """
    pass
